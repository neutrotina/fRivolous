% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_custom_scripts.r
\name{load_custom_scripts}
\alias{load_custom_scripts}
\title{Source custom R scripts into the global environment}
\usage{
load_custom_scripts(script_files)
}
\arguments{
\item{script_files}{A non-empty character vector of paths to R scripts.}
}
\value{
Invisibly returns a list with components:
\describe{
\item{loaded}{Character vector of scripts sourced successfully.}
\item{missing}{Character vector of scripts that did not exist.}
\item{failed}{Character vector of scripts that existed but produced an
error during sourcing.}
}
}
\description{
Sources a collection of R scripts into \code{.GlobalEnv} and reports which files
loaded successfully, were missing, or failed during sourcing.
}
\details{
Script paths are trimmed, expanded with \code{\link[=path.expand]{path.expand()}} and deduplicated
before loading.
}
\section{Side effects}{

Existing scripts are sourced into \code{.GlobalEnv}. Scripts may therefore create,
modify or remove objects in the global environment.
}

\examples{
example_script <- tempfile(
  fileext = ".R"
)

writeLines(
  "example_value <- 42",
  example_script
)

load_result <- load_custom_scripts(
  script_files = example_script
)

example_value

rm(
  example_value,
  envir = .GlobalEnv
)
}
