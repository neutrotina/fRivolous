% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective_complete.r
\name{objective_complete}
\alias{objective_complete}
\title{Mark an analysis session as complete}
\usage{
objective_complete(log_file = NULL, show_session_info = TRUE)
}
\arguments{
\item{log_file}{Optional path to a session log file. Parent directories are
created automatically if they do not exist. If \code{NULL}, the session
information is returned but not written to disk.}

\item{show_session_info}{Logical; should the captured session information
also be printed to the console?}
}
\value{
Invisibly returns a list with components:
\describe{
\item{timestamp}{A \code{POSIXct} timestamp recording when the function ran.}
\item{log_file}{The expanded log-file path, or \code{NULL} if no log file was
requested.}
\item{session_info}{A character vector containing the output of
\code{\link[utils:sessionInfo]{utils::sessionInfo()}}.}
}
}
\description{
Records the current time and R session information, optionally writes the
session information to a log file, and prints a completion message.
}
\details{
This function is intended as a lightweight reproducibility checkpoint at
the end of an analysis session.
}
\section{Side effects}{

If \code{log_file} is supplied, a text file containing the timestamp and session
information is written to disk. Progress and completion messages are printed
to the console.
}

\examples{
session_record <- objective_complete(
  show_session_info = FALSE
)

log_path <- file.path(
  tempdir(),
  "analysis_logs",
  "session_info.txt"
)

session_record <- objective_complete(
  log_file = log_path,
  show_session_info = FALSE
)
}
