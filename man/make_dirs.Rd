% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dir_map.r
\name{make_dirs}
\alias{make_dirs}
\title{Create a directory tree and optionally assign directory paths}
\usage{
make_dirs(
  directory_tree,
  parent = getwd(),
  assign_global = TRUE,
  aliases = character(),
  print_sub_dirs = TRUE,
  tree = FALSE,
  show_files = TRUE
)
}
\arguments{
\item{directory_tree}{A named nested list describing the directory tree.
Leaf directories may be represented by \code{NULL}.}

\item{parent}{Root directory under which the tree will be created.}

\item{assign_global}{Logical; should generated directory paths be assigned
as variables in \code{.GlobalEnv}?}

\item{aliases}{A named character vector of aliases. Names may refer to a
directory name or to a path relative to \code{parent}, such as
\code{"results/figures"}.}

\item{print_sub_dirs}{Logical; should the generated directory variables be
printed?}

\item{tree}{Logical; should the complete directory tree be printed after
creation?}

\item{show_files}{Logical; when \code{tree = TRUE}, should files be included
in the printed directory tree? This does not affect directory creation.}
}
\value{
Invisibly returns a list with components:
\describe{
\item{table}{A data frame recording each created directory, relative path,
absolute path, generated variable and depth.}
\item{paths}{A named character vector of generated directory paths.}
}
}
\description{
Creates the directories described by a named nested list. Each directory
receives a generated variable name based on its path. Directory paths can
optionally be assigned to the global environment.
}
\examples{
project_dirs <- list(
  data = list(
    raw = NULL,
    processed = NULL
  ),
  results = list(
    figures = NULL,
    tables = NULL
  )
)

example_root <- file.path(
  tempdir(),
  "make_dirs_example"
)

created_dirs <- make_dirs(
  directory_tree = project_dirs,
  parent = example_root,
  assign_global = FALSE,
  print_sub_dirs = FALSE,
  tree = TRUE,
  show_files = FALSE
)
}
