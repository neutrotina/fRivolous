% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_and_scan_data.r
\name{load_data_files}
\alias{load_data_files}
\title{Load multiple data files}
\usage{
load_data_files(
  file_list,
  assign_to_global = TRUE,
  print_dims = TRUE,
  print_parameters = TRUE,
  envir = .GlobalEnv
)
}
\arguments{
\item{file_list}{A named character vector or named list of file paths.
Names are used as the names of the imported objects.}

\item{assign_to_global}{Logical; should successfully imported objects be
assigned to \code{envir}?}

\item{print_dims}{Logical; should dimensions or element counts be printed
for successfully imported objects?}

\item{print_parameters}{Logical; should column or element names be printed?}

\item{envir}{Environment in which imported objects should be assigned when
\code{assign_to_global = TRUE}.}
}
\value{
Invisibly returns a list with components:
\describe{
\item{data}{A named list containing successfully imported objects.}
\item{missing}{Names of files that did not exist.}
\item{failed}{Names of files that could not be imported.}
}
}
\description{
Imports RDS, CSV, TSV and TXT files from a named character vector or named
list of file paths. Missing and failed files are recorded separately rather
than aborting the complete import.
}
\section{Supported formats}{

Supported file extensions are \code{.rds}, \code{.csv}, \code{.tsv} and \code{.txt}.
}

\section{Side effects}{

If \code{assign_to_global = TRUE}, successfully imported objects are assigned to
the specified environment.
}

\examples{
example_data <- data.frame(
  sample = c("A", "B"),
  value = c(10, 20)
)

example_file <- file.path(
  tempdir(),
  "example_data.csv"
)

utils::write.csv(
  example_data,
  example_file,
  row.names = FALSE
)

loaded <- load_data_files(
  file_list = c(example = example_file),
  assign_to_global = FALSE,
  print_dims = FALSE,
  print_parameters = FALSE
)
}
