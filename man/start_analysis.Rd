% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_session.r
\name{start_analysis}
\alias{start_analysis}
\title{Start an analysis session}
\usage{
start_analysis(project = getwd(), log_file = NULL, activate_renv = TRUE)
}
\arguments{
\item{project}{Project directory. It must already exist.}

\item{log_file}{Optional path to the analysis log. If \code{NULL}, a timestamped
log is created under \code{project/logs}.}

\item{activate_renv}{Logical; should \code{\link[renv:activate]{renv::activate()}} be called?}
}
\value{
An object of class \code{"Analysis_session"} containing the project path, log path, start time, output connection and initial Git
state.
}
\description{
Activates the project-local \code{renv} environment when requested, records the
initial Git state and starts writing console output to a log file while
continuing to display it on screen.
}
\details{
The returned session object must be passed to \code{\link[=end_analysis]{end_analysis()}} to close the
sink safely.
}
\section{Side effects}{

The project-local \code{renv} may be activated, a log file may be created and an
output sink is started.
}

\examples{
\dontrun{
session <- start_analysis(
  project = getwd(),
  log_file =
    c("logs",
    "analysis_session.log")
)
}

}
