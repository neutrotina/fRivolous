% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_session.r
\name{end_analysis}
\alias{end_analysis}
\title{End an analysis session}
\usage{
end_analysis(
  session,
  snapshot_renv = FALSE,
  commit = FALSE,
  commit_files = character(),
  commit_message = NULL
)
}
\arguments{
\item{session}{An object returned by \code{\link[=start_analysis]{start_analysis()}}.}

\item{snapshot_renv}{Logical; should \code{\link[renv:snapshot]{renv::snapshot()}} be called before
completing the session?}

\item{commit}{Logical; should the explicitly listed files be staged and
committed?}

\item{commit_files}{Character vector of project-relative files or
directories to stage. Required when \code{commit = TRUE}.}

\item{commit_message}{Commit message. Required when \code{commit = TRUE}.}
}
\value{
Invisibly returns a list containing completion time, session log
path, \code{objective_complete()} output and Git commit information.
}
\description{
Completes an analysis session using \code{\link[=objective_complete]{objective_complete()}}, closes the analysis log sink, and optionally snapshots \code{renv} and creates an explicitly scoped Git commit.
}
\section{Side effects}{

The output sink is closed. When requested, \code{renv::snapshot()} modifies
\code{renv.lock}, and Git files are staged and committed.
}

\examples{
\dontrun{
end_analysis(
  session = session,
  snapshot_renv = FALSE,
  commit = TRUE,
  commit_files = c(
    "R/",
  "tests/",
    "renv.lock"
  ),
  commit_message = "analysis: update utilities"
)
}

}
