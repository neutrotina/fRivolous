% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_log_drive.r
\name{clean_log_drive}
\alias{clean_log_drive}
\title{Archive an existing output file}
\usage{
clean_log_drive(target_dir = getwd(), object_file)
}
\arguments{
\item{target_dir}{Directory containing the output file. Defaults to the
current working directory. The directory and its \code{FAILURE_ARCHIVE}
subdirectory are created if necessary.}

\item{object_file}{File path whose basename should be searched for in
\code{target_dir}. Only the basename is used as the target filename.}
}
\value{
Invisibly returns a list with components:
\describe{
\item{archived}{Logical; whether an existing file was moved to the
archive.}
\item{source}{Path of the expected target file in \code{target_dir}.}
\item{archive}{Path of the archived file, or \code{NA_character_} if no file
was present.}
}
}
\description{
Checks whether a named output file exists in a target directory. If it
exists, the file is moved into a \code{FAILURE_ARCHIVE} subdirectory with a
timestamped filename. If no file exists, the target directory and archive
directory are still created and the function reports that the drive is
clear.
}
\section{Side effects}{

The target directory and \code{FAILURE_ARCHIVE} subdirectory may be created.
Existing target files are moved into the archive directory and are not
overwritten.
}

\examples{
example_dir <- file.path(
  tempdir(),
  "clean_log_drive_example"
)

example_file <- file.path(
  example_dir,
  "analysis_output.rds"
)

dir.create(
  example_dir,
  recursive = TRUE,
  showWarnings = FALSE
)

writeLines(
  "previous analysis output",
  example_file
)

archive_result <- clean_log_drive(
  target_dir = example_dir,
  object_file = example_file
)
}
